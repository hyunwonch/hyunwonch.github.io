<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://hyunwon.blog/feed.xml" rel="self" type="application/atom+xml"/><link href="https://hyunwon.blog/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-20T03:33:31+00:00</updated><id>https://hyunwon.blog/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">The Evolution of Attention - From MHA to Kimi K3</title><link href="https://hyunwon.blog/blog/2026/evolution-of-attention/" rel="alternate" type="text/html" title="The Evolution of Attention - From MHA to Kimi K3"/><published>2026-08-19T12:00:00+00:00</published><updated>2026-08-19T12:00:00+00:00</updated><id>https://hyunwon.blog/blog/2026/evolution-of-attention</id><content type="html" xml:base="https://hyunwon.blog/blog/2026/evolution-of-attention/"><![CDATA[<p>Attention is the core operation of the Transformer, and it is also its main scaling bottleneck. Almost every architectural change in large language models over the past decade — MQA, GQA, sliding windows, MLA, sparse attention, and linear attention — exists to fight the same two enemies: the <strong>quadratic compute cost</strong> of attention over sequence length, and the <strong>KV cache</strong> that dominates memory and bandwidth during inference.</p> <p>This post walks through that evolution in order, from the original scaled dot-product attention (2017) to Kimi Delta Attention in Kimi K3 (2026), and ends with a table of which model uses which mechanism.</p> <pre><code class="language-mermaid">timeline
    title A decade of attention mechanisms
    2017 : Scaled dot-product &amp; Multi-Head Attention (Transformer)
    2019 : Multi-Query Attention (MQA)
         : Sparse patterns (Sparse Transformer)
    2020 : Sliding-window attention (Longformer)
         : Linear attention (Linear Transformers)
    2023 : Grouped-Query Attention (GQA)
         : GQA + sliding window in production (Mistral 7B)
    2024 : Multi-head Latent Attention (MLA, DeepSeek-V2)
    2025 : Trainable sparse attention (NSA, DSA)
         : Hybrid linear stacks (MiniMax, Qwen3-Next, Kimi Linear)
    2026 : Kimi Delta Attention + AttnRes at frontier scale (Kimi K3)
</code></pre> <hr/> <h2 id="1-scaled-dot-product-attention-2017">1. Scaled Dot-Product Attention (2017)</h2> <p>The starting point is the attention operation from <em>Attention Is All You Need</em>. Each token produces a query \(q\), a key \(k\), and a value \(v\), and the output is a weighted average of values, where the weights come from query–key similarity:</p> \[\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{QK^{\top}}{\sqrt{d_k}}\right)V\] <p>Note that \(Q\), \(K\), and \(V\) are not the input itself — they are <strong>learned projections</strong> of it. The input hidden states \(X\) are multiplied by three weight matrices \(W^Q\), \(W^K\), \(W^V\) to produce queries (“what am I looking for?”), keys (“what do I contain?”), and values (“what do I hand over?”):</p> <pre><code class="language-mermaid">graph BT
    X["Input hidden states X&lt;br&gt;(n tokens × d_model)"] --&gt;|"× W_Q"| Q["Q — queries&lt;br&gt;(n × d_k)"]
    X --&gt;|"× W_K"| K["K — keys&lt;br&gt;(n × d_k)"]
    X --&gt;|"× W_V"| V["V — values&lt;br&gt;(n × d_v)"]
    Q --&gt; S["scores = Q·Kᵀ / √d_k&lt;br&gt;(n × n)"]
    K --&gt; S
    S --&gt; SM["softmax →&lt;br&gt;attention weights"]
    SM --&gt; O["weights · V"]
    V --&gt; O
    O --&gt; OUT["Attention output&lt;br&gt;(n × d_v)"]
</code></pre> <p>The \(\sqrt{d_k}\) scaling keeps the dot products from saturating the softmax. The important property — and the root of every problem that follows — is that every token attends to every previous token, so both compute and the attention matrix grow as \(O(n^2)\) in sequence length \(n\).</p> <h2 id="2-multi-head-attention-mha">2. Multi-Head Attention (MHA)</h2> <p>The same paper introduced <strong>Multi-Head Attention</strong>: instead of one attention over the full model dimension, project the input into \(h\) smaller subspaces, run attention independently in each, and concatenate:</p> \[\text{MHA}(X) = \text{Concat}(\text{head}_1, \ldots, \text{head}_h)\,W^{O}, \quad \text{head}_i = \text{Attention}(XW_i^{Q},\, XW_i^{K},\, XW_i^{V})\] <p>Each head can specialize — one tracks syntax, another tracks long-range coreference, and so on. Every head \(i\) carries its <strong>own</strong> projection triple \(W_i^Q, W_i^K, W_i^V\), and a final matrix \(W^O\) mixes the concatenated heads back into the model dimension:</p> <pre><code class="language-mermaid">graph BT
    X["Input X"] --&gt;|"W_Q1, W_K1, W_V1"| H1["Head 1&lt;br&gt;Attention(Q1, K1, V1)"]
    X --&gt;|"W_Q2, W_K2, W_V2"| H2["Head 2&lt;br&gt;Attention(Q2, K2, V2)"]
    X --&gt;|"..."| HD["···"]
    X --&gt;|"W_Qh, W_Kh, W_Vh"| HH["Head h&lt;br&gt;Attention(Qh, Kh, Vh)"]
    H1 --&gt; C["Concat all heads"]
    H2 --&gt; C
    HD --&gt; C
    HH --&gt; C
    C --&gt;|"× W_O"| O["MHA output"]
</code></pre> <p>Because every head has its own \(W_i^K\) and \(W_i^V\), every head contributes its own K and V vectors to the cache. That is fine for training, but it defines the inference problem that the next decade of designs tries to solve.</p> <h3 id="the-kv-cache-problem">The KV cache problem</h3> <p>During autoregressive decoding, the keys and values of all past tokens are cached so they are not recomputed at every step. The cache size per token is:</p> \[\text{KV bytes/token} = 2 \times n_{\text{layers}} \times n_{\text{kv-heads}} \times d_{\text{head}} \times \text{bytes/param}\] <p>For a Llama-2-7B-shaped MHA model (32 layers, 32 KV heads, head dim 128, FP16) that is about <strong>0.5 MB per token</strong> — roughly <strong>64 GB for a 128K context</strong>, before you store a single model weight. Decoding is memory-bandwidth-bound: every generated token has to stream the whole cache through the GPU. Shrinking the KV cache is therefore the central theme of everything below.</p> <h2 id="3-multi-query-attention-mqa-2019">3. Multi-Query Attention (MQA, 2019)</h2> <p>Noam Shazeer’s <em>Fast Transformer Decoding</em> proposed the bluntest possible fix: keep all \(h\) query heads, but share <strong>one single K/V head</strong> across all of them. The KV cache shrinks by a factor of \(h\) (32× in the example above), and decoding gets dramatically faster.</p> <p>The cost is quality. All heads are forced to look through the same key/value lens, and MQA models show a measurable quality drop and training instability at scale. PaLM and StarCoder shipped with MQA, but it never became the default.</p> <h2 id="4-grouped-query-attention-gqa-2023">4. Grouped-Query Attention (GQA, 2023)</h2> <p><strong>GQA</strong> (Ainslie et al., Google) is the interpolation between the two extremes: divide the \(h\) query heads into \(g\) groups, and give each group one shared K/V head. With \(g = h\) you recover MHA; with \(g = 1\) you recover MQA. In practice a small number of KV heads (typically 8) recovers almost all of MHA’s quality while keeping most of MQA’s memory savings — Llama 3 70B uses 64 query heads and 8 KV heads, an 8× cache reduction.</p> <p>In the diagram below, solid arrows are projections (\(X\) times a weight matrix produces a Q or K/V head) and dotted arrows show which K/V head each query head attends with. Only the K/V nodes end up in the cache — the query heads are recomputed every step and never stored:</p> <pre><code class="language-mermaid">graph BT
    subgraph MQA["MQA — 4 query projections, 1 shared W_K / W_V"]
        direction BT
        XC["X"] --&gt;|"W_Q1"| C1["Q1"]
        XC --&gt;|"W_Q2"| C2["Q2"]
        XC --&gt;|"W_Q3"| C3["Q3"]
        XC --&gt;|"W_Q4"| C4["Q4"]
        XC --&gt;|"W_K, W_V"| CK1["K, V — 1 head cached"]
        C1 -.-&gt; CK1
        C2 -.-&gt; CK1
        C3 -.-&gt; CK1
        C4 -.-&gt; CK1
    end
    subgraph GQA["GQA — 4 query projections, 1 W_K / W_V per group"]
        direction BT
        XB["X"] --&gt;|"W_Q1"| B1["Q1"]
        XB --&gt;|"W_Q2"| B2["Q2"]
        XB --&gt;|"W_Q3"| B3["Q3"]
        XB --&gt;|"W_Q4"| B4["Q4"]
        XB --&gt;|"W_KA, W_VA"| BK1["K_A, V_A"]
        XB --&gt;|"W_KB, W_VB"| BK2["K_B, V_B"]
        B1 -.-&gt; BK1
        B2 -.-&gt; BK1
        B3 -.-&gt; BK2
        B4 -.-&gt; BK2
    end
    subgraph MHA["MHA — every head has its own W_Q, W_K, W_V"]
        direction BT
        XA["X"] --&gt;|"W_Q1"| A1["Q1"]
        XA --&gt;|"W_Q2"| A2["Q2"]
        XA --&gt;|"W_Q3"| A3["Q3"]
        XA --&gt;|"W_Q4"| A4["Q4"]
        XA --&gt;|"W_K1, W_V1"| AK1["K1, V1"]
        XA --&gt;|"W_K2, W_V2"| AK2["K2, V2"]
        XA --&gt;|"W_K3, W_V3"| AK3["K3, V3"]
        XA --&gt;|"W_K4, W_V4"| AK4["K4, V4"]
        A1 -.-&gt; AK1
        A2 -.-&gt; AK2
        A3 -.-&gt; AK3
        A4 -.-&gt; AK4
    end
</code></pre> <h3 id="gqa-vs-mqa-one-knob-not-two-mechanisms">GQA vs MQA: one knob, not two mechanisms</h3> <p>It is tempting to think of MQA and GQA as different mechanisms, but they differ in exactly one number: the count of K/V heads \(g\). In MQA there is a single \(W^K\)/\(W^V\) pair for the whole layer, so all query heads retrieve information through one shared “view” of the context. In GQA each group of query heads gets its own \(W^K\)/\(W^V\) pair, so the model keeps several distinct views. MHA and MQA are just the two endpoints of the same knob:</p> <table> <thead> <tr> <th style="text-align: center">KV heads \(g\)</th> <th style="text-align: left">Name</th> <th style="text-align: center">Distinct K/V “views”</th> <th style="text-align: center">KV cache vs MHA</th> </tr> </thead> <tbody> <tr> <td style="text-align: center">\(g = h\) (e.g. 32)</td> <td style="text-align: left">MHA</td> <td style="text-align: center">32</td> <td style="text-align: center">1×</td> </tr> <tr> <td style="text-align: center">\(1 &lt; g &lt; h\) (e.g. 8)</td> <td style="text-align: left">GQA</td> <td style="text-align: center">8</td> <td style="text-align: center">4× smaller</td> </tr> <tr> <td style="text-align: center">\(g = 1\)</td> <td style="text-align: left">MQA</td> <td style="text-align: center">1</td> <td style="text-align: center">32× smaller</td> </tr> </tbody> </table> <p>The key empirical finding of the GQA paper is that quality degrades <strong>very non-linearly</strong> along this knob: cutting KV heads from 32 to 8 loses almost nothing, while going from 8 to 1 causes most of MQA’s quality drop. Since 8 KV heads already captures ~80–90% of MQA’s memory savings, the extra compression of full MQA is simply not worth it — which is why MQA (PaLM, StarCoder) was largely abandoned once GQA appeared.</p> <p>A practical bonus: a GQA model does not need to be trained from scratch. The paper showed that an existing MHA checkpoint can be <strong>converted</strong> by mean-pooling each group’s K/V projection matrices into one shared pair, followed by a short “uptraining” run (~5% of the original compute) to recover quality. This is how the first GQA versions of T5 were produced.</p> <p>GQA became the industry default from 2023 to today: Llama 2/3/4, Mistral, Mixtral, Qwen 2/3, Gemma, and GPT-OSS all use it.</p> <h2 id="5-sliding-windows-sinks-and-sparse-attention">5. Sliding Windows, Sinks, and Sparse Attention</h2> <p>A parallel line of work attacks the \(O(n^2)\) term itself by not attending to everything.</p> <ul> <li><strong>Sliding-window (local) attention</strong> — each token attends only to the last \(w\) tokens (Longformer, 2020). Mistral 7B popularized it in production (4K window), and Gemma 2/3 alternate local sliding-window layers with occasional global layers so information can still travel far through depth.</li> <li><strong>Attention sinks</strong> — StreamingLLM observed that softmax attention dumps probability mass on the first tokens; keeping a few “sink” tokens always visible stabilizes windowed attention. GPT-OSS (2025) bakes a learned sink into every attention layer and alternates dense and 128-token sliding-window layers.</li> <li><strong>Trainable sparse attention</strong> — instead of a fixed window, learn which blocks of the context each query should visit. DeepSeek’s <strong>NSA</strong> (Native Sparse Attention, early 2025) and the <strong>DSA</strong> sparse attention deployed in DeepSeek-V3.2 select a small top-\(k\) set of key blocks per query, making long-context prefill and decoding near-linear in practice while remaining trainable end-to-end.</li> </ul> <h2 id="6-multi-head-latent-attention-mla-2024">6. Multi-head Latent Attention (MLA, 2024)</h2> <p>GQA reduces the cache by deleting heads. <strong>MLA</strong>, introduced in DeepSeek-V2, reduces it by <strong>compressing</strong> them. Instead of caching full K and V per head, the hidden state is projected down into one small shared latent vector \(c^{KV}_t\) (512 dims in DeepSeek-V2/V3, versus 32K+ dims of full MHA cache), and per-head K and V are reconstructed by up-projection when needed. A small decoupled component carries RoPE positional information alongside the latent.</p> <pre><code class="language-mermaid">graph BT
    H["Hidden state h_t&lt;br&gt;(7168 dims)"] --&gt;|"W_DKV&lt;br&gt;down-projection"| L[("Latent c_KV (512 dims)&lt;br&gt;the only thing cached")]
    H --&gt;|"W_KR + RoPE"| R[("k_RoPE (64 dims)&lt;br&gt;cached")]
    H --&gt;|"W_Q&lt;br&gt;(low-rank as well)"| Q["Q heads"]
    L --&gt;|"W_UK&lt;br&gt;up-projection per head"| K["K heads"]
    L --&gt;|"W_UV&lt;br&gt;up-projection per head"| V["V heads"]
    Q --&gt; ATT["Attention with&lt;br&gt;full head count"]
    K --&gt; ATT
    V --&gt; ATT
    R --&gt; ATT
</code></pre> <p>The trick is that the up-projections can be folded into the query and output projections at inference time, so the model behaves like MQA for memory purposes while keeping the expressiveness of many distinct heads. DeepSeek reports a <strong>93% KV cache reduction</strong> versus MHA with quality <em>better</em> than MHA, not worse. DeepSeek-V2/V3/R1 and Kimi K2 (a 1T-parameter MoE) all use MLA.</p> <h2 id="7-linear-attention-and-the-delta-rule">7. Linear Attention and the Delta Rule</h2> <p>The most radical branch removes the softmax entirely. If the attention weights are a plain dot product of feature maps, the computation can be rewritten as a <strong>recurrent state update</strong> — a fixed-size matrix \(S_t\) that summarizes the whole past:</p> \[S_t = S_{t-1} + v_t k_t^{\top}, \qquad o_t = S_t\, q_t\] <p>Compute per token is \(O(1)\) regardless of context length, and there is <strong>no KV cache at all</strong> — just the state. The problem is memory quality: naively summing outer products means the state can never forget or correct itself, and early linear attention lagged softmax badly on recall.</p> <p>Two refinements fixed much of that:</p> <ol> <li><strong>The delta rule (DeltaNet)</strong> — instead of blindly adding, first <em>erase</em> what the state currently predicts for key \(k_t\), then write the new value: \(S_t = S_{t-1}\,(I - \beta_t k_t k_t^{\top}) + \beta_t v_t k_t^{\top}\). This turns the state into something the model actively edits rather than a running sum.</li> <li><strong>Gating (Gated DeltaNet)</strong> — add a learned decay so stale information fades: \(S_t = S_{t-1}\,\alpha_t (I - \beta_t k_t k_t^{\top}) + \beta_t v_t k_t^{\top}\).</li> </ol> <p>Pure linear attention still struggles with exact retrieval, so 2025 models converged on <strong>hybrids</strong>: mostly-linear layers with a few full-attention layers interleaved. MiniMax-01/M1 (Lightning attention, 7:1), Qwen3-Next (Gated DeltaNet + gated full attention, 3:1), and Kimi Linear all follow this pattern.</p> <h2 id="8-kimi-delta-attention-and-kimi-k3-2026">8. Kimi Delta Attention and Kimi K3 (2026)</h2> <p><strong>Kimi Delta Attention (KDA)</strong>, introduced in Kimi Linear (late 2025) and scaled up in <strong>Kimi K3</strong>, is the current end of this lineage. KDA refines the gated delta rule in two ways:</p> <ul> <li><strong>Channel-wise forget gates.</strong> Instead of one scalar decay per head, KDA applies a per-channel diagonal decay \(\text{Diag}(\alpha_t)\), so each dimension of the state can be forgotten at its own rate — much finer memory control than Gated DeltaNet.</li> <li><strong>Numerical stability at scale.</strong> Recurrent linear attention tends to overflow when trained at trillion-parameter scale; KDA lower-bounds the log-decay with a scaled sigmoid to keep the recurrence stable.</li> </ul> <p>Kimi K3 itself is a 2.8T-parameter MoE with a 1M-token context window. Its attention stack alternates <strong>three KDA layers for every one Gated MLA (full attention) layer</strong>, and adds <strong>Attention Residuals (AttnRes)</strong> — residual paths on the attention outputs that help information flow through very deep stacks. The linear layers carry no KV cache, so the hybrid cuts KV memory by roughly <strong>75%</strong> and reaches up to <strong>~6× faster decoding at 1M-token context</strong> compared with a full-attention baseline.</p> <pre><code class="language-mermaid">graph BT
    X[Token stream] --&gt; B1
    subgraph BLOCK["Kimi K3 repeating block (3 : 1 hybrid)"]
        B1["KDA layer — linear, O(1) state, no KV cache"] --&gt; B2["KDA layer — linear, O(1) state, no KV cache"]
        B2 --&gt; B3["KDA layer — linear, O(1) state, no KV cache"]
        B3 --&gt; B4["Gated MLA layer — full attention, compressed latent KV cache"]
    end
    B4 --&gt;|"+ AttnRes (attention residual)"| Y[Next block × N]
</code></pre> <p>The overall arc is easy to state: MQA/GQA shrank the cache by <strong>sharing</strong> it, MLA by <strong>compressing</strong> it, sparse attention by <strong>skipping</strong> most of it, and linear attention by <strong>replacing</strong> it with a fixed-size state. Kimi K3 is the first frontier-scale model to bet on the last option as the default, keeping just enough full attention around for exact recall.</p> <pre><code class="language-mermaid">graph BT
    SDPA["Scaled dot-product&lt;br&gt;attention (2017)"] --&gt; MHA["MHA (2017)"]
    MHA --&gt; MQA["MQA (2019)&lt;br&gt;share 1 KV head"]
    MQA --&gt; GQA["GQA (2023)&lt;br&gt;share KV per group"]
    MHA --&gt; SW["Sliding window /&lt;br&gt;sparse (2019–2025)"]
    GQA --&gt; MLA["MLA (2024)&lt;br&gt;compress KV to latent"]
    MHA --&gt; LIN["Linear attention (2020)&lt;br&gt;no KV cache"]
    LIN --&gt; DELTA["DeltaNet → Gated DeltaNet&lt;br&gt;erase + decay"]
    DELTA --&gt; KDA["KDA (2025)&lt;br&gt;channel-wise gates"]
    MLA --&gt; K3["Kimi K3 (2026)&lt;br&gt;KDA : Gated MLA = 3 : 1&lt;br&gt;+ AttnRes"]
    KDA --&gt; K3
</code></pre> <hr/> <h2 id="which-model-uses-which-attention">Which Model Uses Which Attention</h2> <p>Closed frontier models (GPT-4/5, Claude, Gemini) do not disclose their attention internals, so the table covers architectures that are publicly documented.</p> <table> <thead> <tr> <th style="text-align: left">Model</th> <th style="text-align: center">Year</th> <th style="text-align: left">Attention type</th> <th style="text-align: left">Notes</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Transformer (original)</td> <td style="text-align: center">2017</td> <td style="text-align: left">MHA</td> <td style="text-align: left">Encoder–decoder, full softmax attention</td> </tr> <tr> <td style="text-align: left">GPT-2 / GPT-3</td> <td style="text-align: center">2019–20</td> <td style="text-align: left">MHA</td> <td style="text-align: left">GPT-3 alternates dense and banded-sparse layers</td> </tr> <tr> <td style="text-align: left">PaLM</td> <td style="text-align: center">2022</td> <td style="text-align: left">MQA</td> <td style="text-align: left">1 shared KV head</td> </tr> <tr> <td style="text-align: left">LLaMA 1</td> <td style="text-align: center">2023</td> <td style="text-align: left">MHA</td> <td style="text-align: left"> </td> </tr> <tr> <td style="text-align: left">StarCoder</td> <td style="text-align: center">2023</td> <td style="text-align: left">MQA</td> <td style="text-align: left"> </td> </tr> <tr> <td style="text-align: left">Llama 2 (70B)</td> <td style="text-align: center">2023</td> <td style="text-align: left">GQA</td> <td style="text-align: left">Smaller sizes kept MHA</td> </tr> <tr> <td style="text-align: left">Mistral 7B</td> <td style="text-align: center">2023</td> <td style="text-align: left">GQA + sliding window</td> <td style="text-align: left">4K local window</td> </tr> <tr> <td style="text-align: left">Llama 3</td> <td style="text-align: center">2024</td> <td style="text-align: left">GQA</td> <td style="text-align: left">8 KV heads at every size</td> </tr> <tr> <td style="text-align: left">DeepSeek-V2</td> <td style="text-align: center">2024</td> <td style="text-align: left">MLA</td> <td style="text-align: left">First latent-compression deployment</td> </tr> <tr> <td style="text-align: left">Gemma 2 / 3</td> <td style="text-align: center">2024–25</td> <td style="text-align: left">GQA + local/global</td> <td style="text-align: left">Alternating sliding-window and global layers</td> </tr> <tr> <td style="text-align: left">DeepSeek-V3 / R1</td> <td style="text-align: center">2024–25</td> <td style="text-align: left">MLA</td> <td style="text-align: left">671B MoE</td> </tr> <tr> <td style="text-align: left">Qwen3</td> <td style="text-align: center">2025</td> <td style="text-align: left">GQA</td> <td style="text-align: left"> </td> </tr> <tr> <td style="text-align: left">MiniMax-01 / M1</td> <td style="text-align: center">2025</td> <td style="text-align: left">Hybrid linear (Lightning)</td> <td style="text-align: left">7 linear : 1 softmax layers</td> </tr> <tr> <td style="text-align: left">Kimi K2</td> <td style="text-align: center">2025</td> <td style="text-align: left">MLA</td> <td style="text-align: left">1T MoE, DeepSeek-V3-style stack</td> </tr> <tr> <td style="text-align: left">GPT-OSS</td> <td style="text-align: center">2025</td> <td style="text-align: left">GQA + sliding window + sinks</td> <td style="text-align: left">Alternating dense / 128-token window, learned sinks</td> </tr> <tr> <td style="text-align: left">Qwen3-Next</td> <td style="text-align: center">2025</td> <td style="text-align: left">Hybrid Gated DeltaNet</td> <td style="text-align: left">3 linear : 1 gated full-attention layers</td> </tr> <tr> <td style="text-align: left">DeepSeek-V3.2</td> <td style="text-align: center">2025</td> <td style="text-align: left">MLA + DSA</td> <td style="text-align: left">Trainable top-k sparse attention on top of MLA</td> </tr> <tr> <td style="text-align: left">Kimi Linear</td> <td style="text-align: center">2025</td> <td style="text-align: left">Hybrid KDA + MLA</td> <td style="text-align: left">3 : 1, introduced KDA</td> </tr> <tr> <td style="text-align: left"><strong>Kimi K3</strong></td> <td style="text-align: center"><strong>2026</strong></td> <td style="text-align: left"><strong>Hybrid KDA + Gated MLA</strong></td> <td style="text-align: left"><strong>3 : 1, AttnRes, 2.8T MoE, 1M context, ~75% KV cache reduction</strong></td> </tr> </tbody> </table> <hr/> <h2 id="takeaways">Takeaways</h2> <ol> <li><strong>The KV cache, not FLOPs, drove most of this history.</strong> MQA, GQA, and MLA are pure inference-economics inventions — the math of attention never changed, only what gets stored.</li> <li><strong>Ideas compose.</strong> Kimi K3 is simultaneously a linear-attention model (KDA), a latent-compression model (MLA), a gated model, and an MoE. Modern architectures stack every trick that survived.</li> <li><strong>Hybrids won over purity.</strong> Pure linear attention failed at recall for five years; the 3:1 hybrid pattern (Qwen3-Next, Kimi Linear, Kimi K3) keeps a minority of full-attention layers as the model’s “exact memory” and lets cheap linear layers do the bulk of the work.</li> </ol>]]></content><author><name></name></author><category term="technical"/><category term="attention"/><category term="transformer"/><category term="LLM"/><category term="architecture"/><summary type="html"><![CDATA[How attention mechanisms evolved from the original multi-head attention to the hybrid linear attention in Kimi K3]]></summary></entry></feed>